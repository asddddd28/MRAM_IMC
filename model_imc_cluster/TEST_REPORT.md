# Cluster 测试报告入口

完整验收结果及 T01–T14 对照表已整理至 [docs/TEST_REPORT.md](docs/TEST_REPORT.md)。

保留此入口以兼容旧链接。运行 `python run_tests.py` 后，本次原始结果位于
`reports/acceptance/summary.json`、`junit.xml` 与 `pytest-output.txt`；
文档中的结果是最近一次人工核验记录，不会由测试脚本自动改写。
